server:
  port: 8060
app:
  sendgrid:
    api-key:
    from-email: hiclinic2021@gmail.com
    from-name: PTC Admin
    tracking-setting:
      substitution-tag: '%open-track%'
mongock:
  enabled: true
  change-logs-scan-package:
    - com.ptc.sendgrid.changlog
